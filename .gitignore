# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
*.so

# Virtual envs
.venv/
venv/
env/

# Local state — memory databases, sqlite indices, embedded Chroma.
# NEVER commit real memory data.
.llm_mem0/
.state/
*.sqlite
*.sqlite-wal
*.sqlite-shm
chroma/
mem0_db/

# Credentials — NEVER commit tokens or keys.
.env
.env.*
*.credentials.json
.credentials.json
auth.json

# Local PII pattern list for tests/test_smoke.py::test_no_persona_leakage.
# The pattern list itself is PII — NEVER commit it.
tests/pii_patterns.local.txt

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
.omc/
.claude/
